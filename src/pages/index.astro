---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
const homeStudies = [
  {
    href: '/pripadove-studie/karierni-portal-automotive',
    category: 'ŠKODA AUTO · kariérní portál',
    title: '40 % uchazečů nenašlo cestu k volným pozicím. Web přitom „vypadal přehledně“.',
    method: 'Eye-tracking + dotazník · N = 102',
    result: '4 z 10 nenašli volné pozice',
  },
  {
    href: '/pripadove-studie/backend-stories',
    category: 'Edukační microsite',
    title: 'Čím delší text, tím méně pozornosti.',
    method: 'Eye-tracking, AOI analýza · cca 120 respondentů',
    result: '92 % zaznamenalo CTA',
  },
  {
    href: '/pripadove-studie/reklamni-bannery-smer-pohledu',
    category: 'Reklamní kreativa',
    title: 'Pohled postavy nasměroval pozornost návštěvníků.',
    method: 'Eye-tracking experiment · N = 104',
    result: 'ηp² = 0,11 · střední efekt',
  },
];

const gifSrc = `${import.meta.env.BASE_URL.replace(/\/$/, '')}/hero.webp`;
const yearSuffix = new Date().getFullYear().toString().slice(-2);

const clientLogos = [
  { src: `${import.meta.env.BASE_URL.replace(/\/$/, '')}/logos/skoda.webp`, w: 1920, h: 630, alt: 'Škoda Auto' , dh: 50 },
  { src: `${import.meta.env.BASE_URL.replace(/\/$/, '')}/logos/fchk.webp`, w: 800, h: 800, alt: 'FC Hradec Králové' , dh: 54 },
  { src: `${import.meta.env.BASE_URL.replace(/\/$/, '')}/logos/uhk.svg`, w: 157, h: 37, alt: 'Univerzita Hradec Králové' },
  { src: `${import.meta.env.BASE_URL.replace(/\/$/, '')}/logos/texopluslogo.webp`, w: 216, h: 46, alt: 'TEXO Plus' },
];
---

<BaseLayout title="UX výzkum, eye tracking a uživatelské testování | UXMIND" description="Uživatelské testování, eye tracking a měření kognitivní zátěže (model EMLI). Objektivní UX výzkum s vědeckým zázemím FIM UHK. Otestujte svůj web na datech.">
  <Navbar />

  <!-- HERO SECTION -->
  <section class="hero-shell relative overflow-hidden" style="background:#FDFDFD;">

    <!-- Background grid: 4 columns × 3 rows, plus markers at intersections -->
    <div class="absolute inset-0 z-0 pointer-events-none" aria-hidden="true"
      style="background-image: linear-gradient(rgba(10,10,10,0.07) 1px, transparent 1px), linear-gradient(90deg, rgba(10,10,10,0.07) 1px, transparent 1px); background-size: 25% 33.333%;">
      <div class="hidden md:block">
      {[0, 25, 50, 75, 100].flatMap(x =>
        [0, 33.333, 66.667, 100].map(y => (
          <div class="absolute" style={`left:${x}%; top:${y}%; width:14px; height:14px; transform:translate(-50%,-50%); background:linear-gradient(rgba(10,10,10,0.22),rgba(10,10,10,0.22)) center/14px 1px no-repeat, linear-gradient(rgba(10,10,10,0.22),rgba(10,10,10,0.22)) center/1px 14px no-repeat;`}></div>
        ))
      )}
      </div>
    </div>

    <!-- Center CTA — pod středovým obrázkem, na ose řádku mřížky se specializacemi -->
    <div class="absolute left-1/2 -translate-x-1/2 -translate-y-1/2 z-[60] hidden md:block" style="top: 83.333%;">
      <a href="/kontakt" class="inline-block bg-brand-black text-brand-white border border-brand-black px-6 py-2 font-mono text-xs tracking-widest uppercase transition-colors duration-200 hover:bg-transparent hover:text-brand-black">
        DOMLUVIT SCHŮZKU
      </a>
    </div>

    <!-- Large headline — top-aligned s horní linkou (stejná výška jako začínala horní tabulka) -->
    <div class="absolute z-10" style="left: var(--desktop-gutter); top: calc(var(--desktop-top-offset) + 3rem); line-height: 1; margin: 0; padding: 0;">
      <h1 class="m-0 p-0 relative" style="line-height:0;">
        <img src={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/logo-2line.svg`} alt="UX Mind" width="264" height="88" style="width: clamp(8.4rem, 16.8vw, 264px); height:auto; display:block;" />
        <!-- Easter egg: hotspot nad modrou tečkou loga + záslepka, když tečka "uteče" -->
        <span id="logo-dot-hotspot" class="hidden md:block" style="position:absolute; left:89%; top:64%; width:14%; height:24%;"></span>
        <span id="logo-dot-patch" style="position:absolute; left:92.4%; top:69%; width:8%; height:14%; background:#FCFCFC; border-radius:50%; display:none;"></span>
      </h1>
      <!-- Mobile-only: first annotation below heading -->
      <p class="md:hidden font-mono tracking-widest text-brand-black/70 mt-3" style="font-size:10px;">VÝZKUM MÍSTO DOMNĚNEK</p>
    </div>

    <!-- Mobile hero image — static, shown immediately -->
    <div class="md:hidden absolute left-0 right-0 flex items-center justify-center" style="top: 33.333%; height: 33.334%;">
      <img src={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/hero-mobile.webp`} alt="UX Mind — UX výzkum a eye tracking" style="width:100%; height:auto; object-fit:contain;" width="800" height="450" fetchpriority="high" decoding="async" />
    </div>

    <!-- Center 3D Brain — desktop only -->
    <div class="hidden md:flex absolute inset-0 items-center justify-center">
      <div id="hero-gif-container" class="relative" style="width: min(48vw, 760px); height: min(50vh, 540px);">
        <!-- Hero GIF - plays once then freezes on last frame -->
        <img
          id="hero-gif"
          src={gifSrc}
          alt="Animovaná neuronová síť — vizualizace propojení UX výzkumu, eye trackingu a dat"
          class="w-full h-full object-contain"
          width="960" height="540" loading="lazy" decoding="async"
        />

        <!-- Annotation: top — text visible in hero below CTA, line goes down into image -->
        <div class="ann absolute hidden md:flex flex-col items-center opacity-0" style="left:50%; top:-30px; transform:translateX(-50%);">
          <p class="font-mono hero-fluid-micro tracking-widest text-center mb-1 max-w-32">VÝZKUM MÍSTO<br/>DOMNĚNEK</p>
          <div class="ann-line w-px bg-brand-black" style="height:0;" data-size="200"></div>
          <div class="flex-shrink-0" style="width:10px;height:10px;border:1px solid #0A0A0A;background:#FCFCFC;border-radius:50%;display:flex;align-items:center;justify-content:center;"><div style="width:4px;height:4px;background:#0A0A0A;border-radius:50%;"></div></div>
        </div>

        <!-- Annotation: left — text left of container, dot at 22% from left -->
        <div class="ann absolute hidden md:flex items-center opacity-0" style="left:22%; top:42%; transform:translate(-100%,-50%);">
          <div class="text-right mr-2">
            <p class="font-mono hero-fluid-micro tracking-widest">DESIGN ZAČÍNÁ<br/>U UŽIVATELE</p>
          </div>
          <div class="ann-line h-px bg-brand-black" style="width:0;"></div>
          <div class="flex-shrink-0" style="width:10px;height:10px;border:1px solid #0A0A0A;background:#FCFCFC;border-radius:50%;display:flex;align-items:center;justify-content:center;"><div style="width:4px;height:4px;background:#0A0A0A;border-radius:50%;"></div></div>
        </div>

        <!-- Annotation: right top — text right of container, dot at 22% from right; top 40% = pod spodní hranou stats boxu -->
        <div class="ann absolute hidden md:flex items-center opacity-0" style="right:22%; top:40%; transform:translate(100%,-50%);">
          <div class="flex-shrink-0" style="width:10px;height:10px;border:1px solid #0A0A0A;background:#FCFCFC;border-radius:50%;display:flex;align-items:center;justify-content:center;"><div style="width:4px;height:4px;background:#0A0A0A;border-radius:50%;"></div></div>
          <div class="ann-line h-px bg-brand-black" style="width:0;" data-size="110"></div>
          <div class="ml-2">
            <p class="font-mono hero-fluid-micro tracking-widest">EYE TRACKING<br/>UKÁŽE, CO<br/>LIDÉ VIDÍ</p>
          </div>
        </div>

        <!-- Annotation: right bottom — text right of container, dot at 22% from right -->
        <div class="ann absolute hidden md:flex items-center opacity-0" style="right:22%; top:65%; transform:translate(100%,-50%);">
          <div class="flex-shrink-0" style="width:10px;height:10px;border:1px solid #0A0A0A;background:#FCFCFC;border-radius:50%;display:flex;align-items:center;justify-content:center;"><div style="width:4px;height:4px;background:#0A0A0A;border-radius:50%;"></div></div>
          <div class="ann-line h-px bg-brand-black" style="width:0;" data-size="110"></div>
          <div class="ml-2">
            <p class="font-mono hero-fluid-micro tracking-widest">MĚŘIT<br/>POCHOPIT<br/>ZLEPŠIT</p>
          </div>
        </div>

        <!-- Annotation: bottom — text below container, dot at 18% from bottom -->
        <div class="ann absolute hidden md:flex flex-col items-center opacity-0" style="left:50%; bottom:35%; transform:translate(-50%,100%);">
          <div class="flex-shrink-0" style="width:10px;height:10px;border:1px solid #0A0A0A;background:#FCFCFC;border-radius:50%;display:flex;align-items:center;justify-content:center;"><div style="width:4px;height:4px;background:#0A0A0A;border-radius:50%;"></div></div>
          <div class="ann-line w-px bg-brand-black" style="height:0;"></div>
          <p class="font-mono hero-fluid-micro tracking-widest text-center mt-1 max-w-24">DATA<br/>MÍSTO DOJMŮ</p>
        </div>

      </div>
    </div><!-- end desktop gif container -->

    <!-- Info box: stats (top right) -->
    <div class="hero-stats-box hidden absolute z-10 border border-brand-black p-4 bg-brand-white/90 backdrop-blur-sm" style="top: calc(var(--desktop-top-offset) + 3rem); left: calc(75% + clamp(12px,1.8vw,32px)); right: var(--desktop-gutter);">
      <div class="flex justify-between items-center mb-3">
        <p class="font-mono text-xs font-medium tracking-widest">UX MIND</p>
        <p class="font-mono text-xs text-brand-black/60">/{yearSuffix}</p>
      </div>
      <div class="space-y-2">
        <div class="flex items-baseline justify-between">
          <p class="font-display font-bold leading-none stat-projects" style="font-size: calc(clamp(24px, 1.2vw + 14px, 32px) * min(var(--fs, 1), 1.08));">50+</p>
          <p class="font-mono hero-fluid-micro text-brand-black/60 tracking-widest">PROJEKTŮ</p>
        </div>
        <div class="flex items-baseline justify-between">
          <p class="font-display font-bold leading-none stat-respondents" style="font-size: calc(clamp(24px, 1.2vw + 14px, 32px) * min(var(--fs, 1), 1.08));">200+</p>
          <p class="font-mono hero-fluid-micro text-brand-black/60 tracking-widest">RESPONDENTŮ</p>
        </div>
        <div class="flex items-baseline justify-between gap-3 border-t border-brand-black/20 pt-2 mt-1">
          <p class="font-display font-bold leading-none stat-clicks" style="font-size: calc(clamp(24px, 1.2vw + 14px, 32px) * min(var(--fs, 1), 1.08));">50%</p>
          <p class="font-mono hero-fluid-micro text-brand-black/60 tracking-widest">VÍCE PROKLIKŮ</p>
        </div>
      </div>
    </div>

    <!-- Core services — vertically centered in 3rd grid rectangle (66.667%–100%) -->
    <div class="hero-spec-list absolute z-10" style="left: var(--desktop-gutter); top: 83.333%; transform: translateY(-50%);">
      <p class="font-mono text-xs tracking-widest mb-4 text-brand-black/60">[ NAŠE SPECIALIZACE ]</p>
      <div class="space-y-3">
        {[
          { num: '01', label: 'UX AUDIT' },
          { num: '02', label: 'TESTOVÁNÍ UŽIVATELŮ' },
          { num: '03', label: 'EYE TRACKING' },
          { num: '04', label: 'TERÉNNÍ MĚŘENÍ' },
          { num: '05', label: 'UX VÝZKUM' },
        ].map((item) => (
          <div class="flex items-center gap-2">
            <p class="font-mono text-xs text-brand-black/60 shrink-0">{item.num}.</p>
            <span class="spec-dashes font-mono text-xs tracking-tight">
              {Array.from({length: 20}).map(() => (
                <span style="color:rgb(200,200,200); transition:color 0.8s ease;">|</span>
              ))}
            </span>
            <p class="font-mono text-xs font-medium tracking-widest whitespace-nowrap">{item.label}</p>
          </div>
        ))}
      </div>
      <div class="flex items-center gap-3 mt-3">
        <p id="local-time" class="font-mono text-xs tracking-widest">PRG --:-- -- &nbsp; --/--/----</p>
      </div>
    </div>

    <!-- Info box: about us — fills right half of bottom grid rectangle -->
    <div id="info-box" class="hidden md:flex absolute z-30 border border-brand-black bg-brand-black text-brand-white flex-col"
      style="left:calc(75% + clamp(12px,1.8vw,32px)); right: var(--desktop-gutter); top: 83.333%; transform: translateY(-50%); padding:clamp(8px,1.1vh,18px) clamp(10px,1.2vw,20px);">
      <div>
        <div class="mb-2">
          <p class="font-mono font-medium tracking-widest bg-brand-white text-brand-black px-1 py-0.5 block w-full" style="font-size:calc(clamp(11px,0.85vw,13px) * min(var(--fs, 1), 1.08));">NE AGENTURA – ODBORNÍCI</p>
        </div>
        <p class="font-mono text-brand-white/90 leading-relaxed text-justify infobox-desc" style="font-size:calc(clamp(11px,0.85vw,13px) * min(var(--fs, 1), 1.08));">
          Jsme tým UX specialistů. Pomáháme firmám pochopit jejich uživatele prostřednictvím dat, výzkumu a eye trackingu.
        </p>
      </div>
      <div class="shrink-0" style="margin-top:clamp(10px,1.4vh,20px);">
        <div class="pt-2 border-t border-brand-white/20 flex flex-col items-start gap-1.5">
          <p class="font-mono text-brand-white/75 tracking-widest whitespace-nowrap" style="font-size:calc(clamp(10px,0.75vw,12px) * min(var(--fs, 1), 1.08));">PROJEKT ZA ÚČASTI:</p>
          <img src={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/logos/uhk.svg`} alt="Univerzita Hradec Králové" class="max-w-full shrink-0" style="height:clamp(24px,2.8vh,40px); object-fit:contain; margin-top:4px; margin-bottom:4px; filter: invert(1);" />
        </div>
      </div>
    </div>

    <!-- Cursor dot — only in hero area -->
    <div id="cursor-dot" style="position:fixed;top:0;left:0;width:8px;height:8px;background:#2D62FC;border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:0;"></div>

    <!-- Scroll indicator -->
    <button type="button" aria-label="Posunout na obsah" class="hidden md:flex absolute bottom-6 z-10 flex-col items-start gap-2 animate-bounce cursor-pointer" style="left: 50%;" onclick="window.scrollBy({ top: window.innerHeight * 0.6, behavior: 'smooth' })">
      <p class="font-mono text-xs text-brand-black/60 tracking-widest" style="transform: translateX(-50%);">SCROLL</p>
      <div class="h-8 bg-brand-black/20" style="width: 1px;"></div>
    </button>
  </section>

  <main id="main-content">

  <!-- Mobile info box — NE AGENTURA, above logo carousel -->
  <div class="md:hidden border border-brand-black bg-brand-black text-brand-white mx-6 my-8 p-6 flex flex-col gap-4">
    <div>
      <p class="font-mono font-medium tracking-widest bg-brand-white text-brand-black px-1 py-0.5 block w-full" style="font-size:12px;">NE AGENTURA – ODBORNÍCI</p>
    </div>
    <p class="font-mono text-sm text-brand-white/90 leading-relaxed text-justify">Jsme tým UX specialistů. Pomáháme firmám pochopit jejich uživatele prostřednictvím dat, výzkumu a eye trackingu.</p>
    <div class="flex gap-2">
      <a href="/o-nas" class="font-mono text-xs border border-brand-white px-2 py-1">O NÁS</a>
      <a href="/kontakt" class="font-mono text-xs border border-brand-white px-2 py-1">KONTAKT</a>
    </div>
    <div class="border-t border-brand-white/20 pt-2 flex items-center justify-between">
      <p class="font-mono text-[11px] text-brand-white/75 tracking-widest">PROJEKT ZA ÚČASTI:</p>
      <img src={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/logos/uhk.svg`} alt="Univerzita Hradec Králové" class="h-[40px] w-auto ml-6 my-1" style="filter: invert(1);" />
    </div>
  </div>

  <!-- LOGO STRIP -->
  <div class="border-y border-brand-black/10 py-6" style="background:#FDFDFD;">
    <!-- Desktop: statický pás -->
    <div class="hidden md:flex flex-wrap items-center justify-center gap-x-14 gap-y-6 px-6">
      {clientLogos.map(({ src, alt, w, h, dh }) => (
        <div class="px-4 py-1">
          <img
            src={src}
            alt={alt}
            title={alt}
            width={w} height={h}
            class="logo-strip-img select-none"
            style={`height:${dh || 42}px; width:auto; max-width:170px; object-fit:contain; display:block;`}
            loading="lazy"
            decoding="async"
          />
        </div>
      ))}
    </div>
    <!-- Mobil: automatický carousel, dvě loga na řádek -->
    <div class="md:hidden overflow-hidden">
      <div class="logo-marquee flex items-center">
        {[...clientLogos, ...clientLogos].map(({ src, alt, w, h, dh }) => (
          <div class="flex-shrink-0 flex justify-center items-center px-4" style="width:50vw;">
            <img
              src={src}
              alt={alt}
              title={alt}
              width={w} height={h}
              class="logo-strip-img select-none"
              style={`height:${dh ? Math.round(dh * 36 / 42) : 36}px; width:auto; max-width:40vw; object-fit:contain; display:block;`}
              loading="lazy"
              decoding="async"
            />
          </div>
        ))}
      </div>
    </div>
  </div>

  <style>
    .logo-strip-img {
      filter: grayscale(1);
      opacity: 0.65;
    }
    .logo-marquee {
      width: max-content;
      /* smyčka je jen vizuální (transform) — loga se stahují jednorázově a běží z cache */
      animation: logo-scroll 10s linear infinite;
    }
    @keyframes logo-scroll {
      from { transform: translateX(0); }
      to { transform: translateX(-150vw); }
    }
    @media (prefers-reduced-motion: reduce) {
      .logo-marquee { animation: none; }
    }
  </style>

  <!-- TŘI OTÁZKY, TŘI MĚŘENÍ -->
  <section class="desktop-gutter-x pt-24 pb-12 max-w-7xl mx-auto">
    <div class="mb-12 max-w-3xl">
      <p class="font-mono text-xs text-brand-black/60 tracking-widest mb-2">[ TŘI OTÁZKY, TŘI MĚŘENÍ ]</p>
      <h2 class="font-display text-4xl md:text-5xl font-bold leading-tight mb-6">Co uživatel vidí, jak reaguje<br/>a co ho zatěžuje<span style="color:#2D62FC">.</span></h2>
      <p class="text-sm text-brand-black/60 leading-relaxed">
        Analytika ukáže, KAM lidé klikli. Dotazník ukáže, CO si myslí. My měříme třetí vrstvu:
        co skutečně viděli, jak silně reagovali a kde je rozhraní mentálně přetížilo.
      </p>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-px bg-brand-black/10">
      {[
        {
          q: 'Vidí to vůbec?',
          desc: 'Vizuální pozornost — heatmapy, fixace, TTFF, dwell time, AOI analýza a scanpathy. Eye tracking objektivně zachytí, co uživatel skutečně viděl a co minul.',
          tool: 'EYE-TRACKING',
        },
        {
          q: 'Jak silně reaguje?',
          desc: 'Fyziologický arousal a mimické signály v kontextu úkolu. Interpretujeme je vždy s limity — žádné „čtení emocí".',
          tool: 'GSR + MIMICKÉ SIGNÁLY',
        },
        {
          q: 'Zvládá to bez námahy?',
          desc: 'Kognitivní zátěž jako jedno srozumitelné skóre 0–100. Zjistíte, kde rozhraní uživatele přetěžuje, i když úkol nakonec splní.',
          tool: 'MODEL EMLI',
        },
      ].map((item, i) => (
        <div class="bg-brand-white p-8 flex flex-col">
          <p class="font-mono text-xs text-brand-black/60 mb-8">0{i + 1}</p>
          <h3 class="font-display text-2xl font-bold mb-4 leading-tight">{item.q}</h3>
          <p class="text-sm text-brand-black/60 leading-relaxed mb-6 text-justify">{item.desc}</p>
          <p class="mt-auto"><span class="font-mono text-xs border border-brand-black/20 px-2 py-1 tracking-widest">{item.tool}</span></p>
        </div>
      ))}
    </div>
    <!-- Slovníček zkratek -->
    <details class="group border border-brand-black/15 mt-6">
        <summary class="flex justify-between items-center gap-6 cursor-pointer p-5 font-mono text-xs tracking-widest list-none [&::-webkit-details-marker]:hidden">
          [ SLOVNÍK POJMŮ ]
          <span class="font-mono text-xl shrink-0 transition-transform group-open:rotate-45" aria-hidden="true">+</span>
        </summary>
        <dl class="px-5 pb-6 space-y-4">
            <div><dt class="font-display font-bold mb-1">TTFF</dt><dd class="text-sm text-brand-black/70 leading-relaxed">čas do první fixace pohledu</dd></div>
            <div><dt class="font-display font-bold mb-1">AOI</dt><dd class="text-sm text-brand-black/70 leading-relaxed">sledovaná oblast zájmu (Area of Interest)</dd></div>
            <div><dt class="font-display font-bold mb-1">dwell time</dt><dd class="text-sm text-brand-black/70 leading-relaxed">celkový čas strávený pohledem v oblasti</dd></div>
            <div><dt class="font-display font-bold mb-1">scanpath</dt><dd class="text-sm text-brand-black/70 leading-relaxed">dráha pohledu mezi fixacemi</dd></div>
            <div><dt class="font-display font-bold mb-1">GSR</dt><dd class="text-sm text-brand-black/70 leading-relaxed">měření kožní vodivosti, indikátor fyziologického arousalu</dd></div>
            <div><dt class="font-display font-bold mb-1">EMLI</dt><dd class="text-sm text-brand-black/70 leading-relaxed">index mentální (kognitivní) zátěže odvozený z eye-tracking metrik</dd></div>
        </dl>
      </details>
  </section>

  <!-- SERVICES SECTION -->
  <section class="desktop-gutter-x py-24 max-w-7xl mx-auto">
    <div class="mb-12 max-w-3xl">
      <p class="font-mono text-xs text-brand-black/60 tracking-widest mb-2">[ NAŠE SLUŽBY ]</p>
      <h2 class="font-display text-4xl md:text-5xl font-bold leading-tight mb-6">Co děláme<span style="color:#2D62FC">.</span></h2>
      <p class="text-sm text-brand-black/60 leading-relaxed">
        Pomáháme zjistit, kde web, aplikace nebo komunikace lidem komplikuje cestu. Problém změříme, vysvětlíme jeho příčinu a doporučíme, co upravit jako první.
      </p>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-px bg-brand-black/10">
      {[
        {
          num: '01',
          title: 'UX Audit',
          desc: 'Projdeme váš web, e-shop nebo aplikaci očima uživatele. Najdeme místa, kde lidé tápou, nedokončí objednávku nebo přehlédnou důležitou informaci. Dostanete seznam úprav seřazený podle priority.',
          href: '/sluzby/ux-audit',
          tags: ['Analýza', 'Doporučení', 'Prioritizace'],
        },
        {
          num: '02',
          title: 'Testování uživatelů',
          desc: 'Dáme reálným uživatelům konkrétní úkoly a sledujeme, zda je dokážou splnit. Uvidíte, kde se ztrácejí, čemu nerozumějí a proč odcházejí — spolu s návrhy, co změnit.',
          href: '/sluzby/testovani-uzivatelu',
          tags: ['Úkolové scénáře', 'Úspěšnost', 'Kvalitativní vhledy'],
        },
        {
          num: '03',
          title: 'Eye Tracking',
          desc: 'Pomocí eye trackingu změříme, kam se lidé skutečně dívají. Heatmapy a záznam pohledu odhalí, čeho si všimnou, co přehlédnou a zda je důležitý obsah vede správným směrem.',
          href: '/sluzby/eye-tracking',
          tags: ['Heatmapa', 'AOI · TTFF', 'Scanpath'],
        },
        {
          num: '04',
          title: 'Terénní měření',
          desc: 'Sledujeme chování zákazníků přímo tam, kde se rozhodují — v prodejně, showroomu, na akci nebo u fyzického produktu. Mobilní eye-tracking brýle ukážou, čeho si v reálném prostředí všimnou a co minou.',
          href: '/sluzby/terenni-mereni',
          tags: ['Mobilní eye-tracking', 'Reálný kontext', 'Pozorování'],
        },
        {
          num: '05',
          title: 'UX Výzkum',
          desc: 'Když potřebujete pochopit nejen co nefunguje, ale také proč. Propojíme rozhovory, pozorování, testování a měření reakcí, abyste mohli rozhodovat podle skutečných potřeb uživatelů.',
          href: '/sluzby/ux-vyzkum',
          tags: ['Eye-tracking', 'Biometrie', 'Dotazníky'],
        },
        {
          num: '06',
          title: 'Případové studie',
          desc: 'Podívejte se, co odhalila skutečná měření: 40 % uchazečů nenašlo cestu k volným pozicím a hlavní CTA zaznamenalo 92 % respondentů. Ukazujeme data i změny, ke kterým vedla.',
          href: '/pripadove-studie',
          tags: ['Výsledky', 'Data', 'Rozhodnutí'],
        },
      ].map(service => (
        <a href={service.href} class="group bg-brand-white p-8 hover:bg-brand-black hover:text-brand-white transition-colors duration-300">
          <div class="flex justify-between items-start mb-8">
            <p class="font-mono text-xs text-brand-black/60 group-hover:text-brand-white/60">{service.num}</p>
            <span class="font-mono text-xs group-hover:text-brand-white/60">→</span>
          </div>
          <h3 class="font-display text-3xl font-bold mb-4 leading-tight">{service.title}</h3>
          <p class="text-sm text-brand-black/60 group-hover:text-brand-white/60 leading-relaxed mb-6 text-justify">{service.desc}</p>
          <div class="flex flex-wrap gap-2">
            {service.tags.map(tag => (
              <span class="font-mono text-xs border border-brand-black/20 group-hover:border-brand-white/20 px-2 py-1">{tag}</span>
            ))}
          </div>
        </a>
      ))}
    </div>
    <!-- Testovací balíčky — černý pás, ať vyniknou -->
    <a href="/sluzby/testovaci-balicky" class="group flex flex-col md:flex-row md:items-center justify-between gap-4 bg-brand-black text-brand-white border border-brand-black p-8 hover:bg-[#2D62FC] transition-colors duration-300">
      <div>
        <p class="font-mono text-xs text-brand-white/60 tracking-widest mb-2">[ TESTOVACÍ BALÍČKY ]</p>
        <p class="font-display text-2xl font-bold leading-tight">Od rychlé kontroly po výzkum do hloubky. Vyberte si rozsah podle toho, co potřebujete zjistit.</p>
      </div>
      <span class="font-mono text-xs tracking-widest uppercase shrink-0">POROVNAT BALÍČKY →</span>
    </a>
  </section>

  <!-- EMLI SECTION -->
  <section class="desktop-gutter-x py-24 bg-brand-black text-brand-white">
    <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
      <div>
        <p class="font-mono text-xs text-brand-white/70 tracking-widest mb-6">[ EMLI — EYE-TRACKING MENTAL LOAD INDEX ]</p>
        <h2 class="font-display text-4xl md:text-6xl font-bold leading-none tracking-tight mb-8">Mentální zátěž<br/>jako číslo<span style="color:#2D62FC">.</span></h2>
        <p class="text-brand-white/60 leading-relaxed mb-6 text-justify">
          Heatmapa ukáže, kam se lidé dívali. Sama ale nepozná, jestli pro ně bylo rozhraní snadné,
          nebo je při plnění úkolu zbytečně zatěžovalo. EMLI spojuje osm eye-tracking metrik do jednoho
          skóre kognitivní zátěže od 0 do 100. <strong class="text-brand-white">Čím vyšší skóre, tím větší zátěž.</strong> Snadno tak porovnáte
          dvě varianty designu, jednotlivé kroky formuláře nebo konkurenční řešení — na datech, ne podle dojmu.
        </p>
        <p class="font-mono text-xs text-brand-white/70 leading-relaxed mb-10">
          Model vznikl v akademickém výzkumu FIM UHK.<br class="hidden md:block"/> Metodika je podrobně zdokumentovaná pro nezávislé ověření.
        </p>
        <a href="/kontakt" class="inline-block border border-brand-white px-8 py-4 font-mono text-xs tracking-widest uppercase hover:bg-brand-white hover:text-brand-black transition-colors">
          CHCI ZMĚŘIT ZÁTĚŽ →
        </a>
      </div>
      <div class="flex justify-center">
        <!-- EMLI gauge 0–100 -->
        <svg viewBox="0 0 320 200" class="w-full max-w-sm" role="img" aria-label="Ilustrace indexu mentální zátěže EMLI na škále 0 až 100">
          <path d="M 30 170 A 130 130 0 0 1 290 170" fill="none" stroke="rgba(252,252,252,0.15)" stroke-width="14" stroke-linecap="round" />
          <path id="emli-arc" d="M 30 170 A 130 130 0 0 1 290 170" pathLength="100" fill="none" stroke="#FCFCFC" stroke-width="14" stroke-linecap="round" stroke-dasharray="65 100" />
          {Array.from({ length: 11 }).map((_, i) => {
            const a = Math.PI - (i * Math.PI) / 10;
            const x1 = 160 + Math.cos(a) * 108, y1 = 170 - Math.sin(a) * 108;
            const x2 = 160 + Math.cos(a) * 98,  y2 = 170 - Math.sin(a) * 98;
            return <line x1={x1} y1={y1} x2={x2} y2={y2} stroke="rgba(252,252,252,0.35)" stroke-width="2" />;
          })}
          <g id="emli-needle" style="transform-box: view-box; transform-origin: 160px 170px; transform: rotate(117deg);">
            <line x1="160" y1="170" x2="76" y2="170" stroke="#FCFCFC" stroke-width="2" />
          </g>
          <circle cx="160" cy="170" r="6" fill="#2D62FC" />
          <text x="30" y="192" fill="rgba(252,252,252,0.5)" font-family="monospace" font-size="12">0</text>
          <text x="278" y="192" fill="rgba(252,252,252,0.5)" font-family="monospace" font-size="12">100</text>
          <text id="emli-value" x="160" y="130" fill="#FCFCFC" text-anchor="middle" font-family="monospace" font-size="38" font-weight="700">65</text>
          <text x="160" y="150" fill="rgba(252,252,252,0.6)" text-anchor="middle" font-family="monospace" font-size="11" letter-spacing="3">EMLI</text>
          <text x="160" y="196" fill="rgba(252,252,252,0.5)" text-anchor="middle" font-family="monospace" font-size="10" letter-spacing="2">8 METRIK → 1 INDEX</text>
        </svg>
      </div>
    </div>
  </section>

  <!-- STATS SECTION -->
  <section class="desktop-gutter-x py-16 border-t border-brand-black/10">
    <div class="max-w-7xl mx-auto grid grid-cols-2 md:grid-cols-4 gap-8">
      {[
        { num: '5+', label: 'Realizovaných studií' },
        { num: '450+', label: 'Respondentů ve studiích' },
        { num: '8', label: 'Metrik v modelu EMLI' },
        { num: '0–100', label: 'Škála indexu zátěže' },
      ].map(stat => (
        <div class="text-center">
          <p class="font-display text-5xl md:text-6xl font-bold mb-3">{stat.num}</p>
          <div class="border-t border-brand-black/15 mb-2"></div>
          <p class="font-mono text-xs text-brand-black/60 tracking-widest uppercase">{stat.label}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- VÝZKUM A TECHNOLOGIE -->
  <section class="desktop-gutter-x py-24 border-t border-brand-black/10 max-w-7xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
      <div>
        <p class="font-mono text-xs text-brand-black/60 tracking-widest mb-2">[ LABORATOŘ A TECHNOLOGIE ]</p>
        <h2 class="font-display text-4xl md:text-5xl font-bold leading-tight mb-6">Technologie nestačí.<br/>Rozhoduje metodika<span style="color:#2D62FC">.</span></h2>
        <p class="text-sm text-brand-black/60 leading-relaxed mb-8 text-justify">
          Měříme v Laboratoři kognitivních studií FIM UHK i přímo v terénu. Moderní přístroje ale samy
          o sobě spolehlivý výsledek nezaručí. Proto každý výzkum začíná jasnou otázkou, scénář ověřujeme
          předem a během měření kontrolujeme kvalitu dat. Výsledky posuzujeme v kontextu konkrétního úkolu
          a otevřeně uvádíme, co z nich lze — a nelze — vyvodit.
        </p>
        <div class="flex flex-wrap gap-2">
          {['TOBII PRO X2-60', 'SMART EYE AURORA', 'PUPIL LABS NEON', 'IMOTIONS', 'SHIMMER3 GSR'].map(tech => (
            <span class="font-mono text-xs border border-brand-black/20 px-2 py-1 tracking-widest">{tech}</span>
          ))}
        </div>
        <p class="font-mono font-medium tracking-widest bg-brand-black text-brand-white text-xs px-2 py-1 block w-full mt-8">AKADEMICKÉ ZÁZEMÍ</p>
        <p class="text-sm text-brand-black/60 leading-relaxed mt-4 text-justify">
          Členové týmu publikují výsledky v odborných časopisech Scientific Reports (Nature Portfolio)
          a Journal of Environmental Psychology i na konferenci ACM ETRA. Stejnou pečlivost přenášíme
          do klientských projektů — od návrhu měření až po srozumitelnou interpretaci výsledků.
        </p>
      </div>
      <div class="flex flex-col">
        <!-- Fotka vyplní výšku sloupce (ořez object-cover) -->
        <div class="border border-brand-black flex flex-col flex-1 min-h-0">
          <img
            src={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/images/jan-petruzalek-analyza-eye-tracking-dat.webp`}
            alt="Výzkumník UXMIND analyzuje eye-tracking data — na monitoru heatmapy vizuální pozornosti, na tabletu záznamy mimických výrazů respondentů"
            title="Analýza eye-tracking dat v Laboratoři kognitivních studií FIM UHK"
            width="1920" height="1280" loading="lazy" decoding="async"
            class="w-full flex-1 min-h-0 object-cover block"
          />
        </div>
        <div class="mt-4 pt-3 border-t border-brand-black/20 flex items-center justify-between">
          <p class="font-mono text-[10px] text-brand-black/60 tracking-widest">VE SPOLUPRÁCI S:</p>
          <img src={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/logos/uhk.svg`} alt="Univerzita Hradec Králové" class="h-[40px] w-auto" />
        </div>
      </div>
    </div>
  </section>

  <!-- PŘÍPADOVÉ STUDIE -->
  <section class="desktop-gutter-x py-24 max-w-7xl mx-auto">
    <div class="flex justify-between items-end mb-12">
      <div>
        <p class="font-mono text-xs text-brand-black/60 tracking-widest mb-2">[ PŘÍPADOVÉ STUDIE ]</p>
        <h2 class="font-display text-4xl md:text-5xl font-bold leading-tight">Co odhalilo<br/>měření<span style="color:#2D62FC">.</span></h2>
      </div>
      <a href="/pripadove-studie" class="font-mono text-xs tracking-widest border-b border-brand-black pb-1 hover:opacity-50 transition-opacity">
        VŠECHNY STUDIE →
      </a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-px bg-brand-black/10 border border-brand-black/10">
      {homeStudies.map(study => (
        <a href={study.href} class="group bg-brand-white p-8 flex flex-col hover:bg-brand-black transition-colors">
          <div class="flex justify-between items-start gap-3 mb-6">
            <span class="font-mono text-xs text-brand-black/60 group-hover:text-brand-white/70 border border-brand-black/20 group-hover:border-brand-white/50 px-2 py-0.5 transition-colors">{study.category}</span>
          </div>
          <h3 class="font-display text-xl font-bold mb-4 leading-tight group-hover:text-brand-white transition-colors">{study.title}</h3>
          <p class="font-mono text-[11px] text-brand-black/60 group-hover:text-brand-white/70 tracking-wide mb-6 transition-colors">METODA: {study.method}</p>
          <div class="mt-auto">
            <span class="block font-display text-lg font-bold leading-none whitespace-nowrap mb-4" style="color:#2D62FC">{study.result}</span>
            <span class="block font-mono text-xs tracking-widest group-hover:text-brand-white transition-colors">PROJÍT STUDII →</span>
          </div>
        </a>
      ))}
    </div>
  </section>

  <!-- JAK TO U NÁS FUNGUJE -->
  <section class="desktop-gutter-x py-24 border-t border-brand-black/10 max-w-7xl mx-auto">
    <div class="flex justify-between items-end mb-12">
      <div>
        <p class="font-mono text-xs text-brand-black/60 tracking-widest mb-2">[ PRŮBĚH SPOLUPRÁCE ]</p>
        <h2 class="font-display text-4xl md:text-5xl font-bold leading-tight">Jak probíhá<br/>spolupráce<span style="color:#2D62FC">.</span></h2>
      </div>
      <a href="/sluzby/testovaci-balicky" class="font-mono text-xs tracking-widest border-b border-brand-black pb-1 hover:opacity-50 transition-opacity">
        TESTOVACÍ BALÍČKY →
      </a>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-5 gap-px bg-brand-black/10 border border-brand-black/10">
      {[
        { num: '01', title: 'Úvodní konzultace', desc: 'Řeknete nám, co potřebujete zjistit nebo rozhodnout. Společně vybereme, koho a co má smysl testovat.' },
        { num: '02', title: 'Návrh výzkumu', desc: 'Doporučíme vhodné metody, počet účastníků, rozsah a podobu výsledků. Předem budete vědět, na které otázky měření odpoví.' },
        { num: '03', title: 'Příprava testování', desc: 'Připravíme úkoly, podklady a dotazníky. U složitějšího projektu vše nejprve ověříme zkušebním měřením.' },
        { num: '04', title: 'Měření', desc: 'Testujeme v laboratoři nebo přímo v terénu. Hlídáme správný průběh i kvalitu zaznamenaných dat.' },
        { num: '05', title: 'Výsledky a doporučení', desc: 'Data propojíme s pozorováním a výpověďmi uživatelů. Dostanete jasné závěry, priority a doporučení, co upravit.' },
      ].map(step => (
        <div class="bg-brand-white p-6 md:p-8">
          <p class="font-mono text-xs text-brand-black/60 mb-6">{step.num}</p>
          <h3 class="font-display text-base font-bold mb-3 whitespace-nowrap">{step.title}</h3>
          <p class="text-sm text-brand-black/60 leading-relaxed">{step.desc}</p>
        </div>
      ))}
    </div>
  </section>

  </main>

  <Footer />
</BaseLayout>

<script>
  import '../scripts/homepage';
</script>
